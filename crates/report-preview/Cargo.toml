[package]
name = "report-preview"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14", features = ["canvas", "image-without-codecs", "smol"] }
report-core = { path = "../report-core" }
report-pdf = { path = "../report-pdf" }
